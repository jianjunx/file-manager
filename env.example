# 应用运行端口
# 默认为8000
PORT=8000

# 身份验证配置
# 如果设置了用户名和密码，则启用身份验证
# 如果不设置，则任何人都可以访问文件管理器
AUTH_USERNAME=admin
AUTH_PASSWORD=your_secure_password

# 可选：Session密钥，用于加密session
# 如果不设置，系统会自动生成
# SESSION_SECRET=your_random_secret_key

# 可选：文件管理器根目录
# Docker部署时会自动使用/data目录
# 本地开发时默认使用当前工作目录
# FILE_MANAGER_ROOT=/path/to/your/files 